apiVersion: v1
kind: Service
metadata:
  name: edge-upf-service
  namespace: edge-upf
spec:
  type: NodePort
  selector:
    app: edge-upf
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30091
